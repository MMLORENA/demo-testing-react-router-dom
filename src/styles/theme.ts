import { DefaultTheme } from "styled-components/dist/types";

const theme: DefaultTheme = {
  colors: {
    base: "#ff8c00",
    dark: "#000",
    light: "#fff",
    neutral: "#91a3b5",
  },
};

export default theme;
